import streamlit as st

st.set_page_config(page_title="Main Regression Hub")

st.title("Main Regression Analysis")
st.write("welcome! use the sidebar to switch between different analysis modes.")
st.write("\n in 'custom synthetic data generation,' choose the different parameters for generating synthetic data, and tune the parameters of the mixture regression model accordingly.")
